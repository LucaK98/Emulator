node_modules/
dist/
coverage/
*.tsbuildinfo

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Intermediate core build output. The finished WASM cores in public/cores/ ARE
# committed, so deploying never needs an Emscripten toolchain.
build/
vendor/**/build/

# Never commit game images or personal save data. Test ROMs live under
# tests/roms/ and are explicitly re-included below.
*.gb
*.gbc
*.gba
*.nds
*.dsi
*.srm
*.sav
*.state
!tests/roms/**/*.gb
!tests/roms/**/*.gbc
!tests/roms/**/*.gba

.DS_Store
.env
.env.local
